[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "php artisan migrate --force && php artisan storage:link && /usr/bin/supervisord -c /etc/supervisord.conf"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
